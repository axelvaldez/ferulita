<div class="row mt-5">
        {% if pagination.href.previous and  pagination.href.next %}
                <div class="col-6">
                        <a href="{{pagination.href.previous }}" class="btn btn-outline-primary">&larr; Episodios más recientes</a>
                </div>
                <div class="col-6 text-end">
                        <a href="{{pagination.href.next }}" class="btn btn-outline-primary">Episodios Anteriores &rarr;</a>
                </div>
        {% elif pagination.href.previous%}
                <div class="col-6">
                        <a href="{{pagination.href.previous }}" class="btn btn-outline-primary">&larr; Episodios más recientes</a>
                </div>
        {% elif pagination.href.next%}
                <div class="col-6 offset-6 text-end">
                        <a href="{{pagination.href.next }}" class="btn btn-outline-primary">Episodios Anteriores &rarr;</a>
                </div>
        {% endif %}
</div>